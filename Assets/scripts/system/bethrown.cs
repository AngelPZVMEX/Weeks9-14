using UnityEngine;

public class bethrown : MonoBehaviour
{
    public Component glove;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
